<%= form_with model: patient, local: true, class: "space-y-4" do |f| %>
  <% if patient.errors.any? %>
    <div class="bg-red-50 border border-red-200 rounded-lg p-4 text-sm text-red-700">
      <p class="font-medium"><%= pluralize(patient.errors.count, "error") %> prevented saving:</p>
      <ul class="list-disc list-inside mt-1">
        <% patient.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :first_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_field :first_name, required: true,
        class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-500 focus:border-brand-500 outline-none" %>
  </div>

  <div>
    <%= f.label :last_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= f.text_field :last_name, required: true,
        class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-500 focus:border-brand-500 outline-none" %>
  </div>

  <%= f.submit class: "w-full py-2 px-4 bg-brand-600 text-white rounded-lg hover:bg-brand-700 cursor-pointer font-medium" %>
<% end %>
