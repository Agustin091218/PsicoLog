<%= render "shared/flash" %>

<div class="max-w-md mx-auto mt-16">
  <h1 class="text-2xl font-semibold mb-6">Iniciar sesión</h1>

  <%= form_with url: sessions_path, scope: :session, local: true, class: "space-y-4" do |f| %>
    <div>
      <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.email_field :email, required: true,
          class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-500 focus:border-brand-500 outline-none" %>
    </div>

    <div>
      <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.password_field :password, required: true,
          class: "w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-500 focus:border-brand-500 outline-none" %>
    </div>

    <%= f.submit "Ingresar", class: "w-full py-2 px-4 bg-brand-600 text-white rounded-lg hover:bg-brand-700 cursor-pointer font-medium" %>
  <% end %>

  <p class="mt-4 text-sm text-gray-500 text-center">
    ¿No tienes cuenta? <%= link_to "Registrarse", new_user_path, class: "text-brand-600 hover:underline" %>
  </p>
</div>
